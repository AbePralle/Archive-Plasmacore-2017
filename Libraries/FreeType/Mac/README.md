This precompiled FreeType2 library was created by running the following in the freetype-2.6.3 folder:

    ./configure
    make
    make install

and then copying out of `/usr/local/include/freetype2/*` and `/usr/local/lib/libfreetype.a`.

